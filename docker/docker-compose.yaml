version: "3.9"

services:
  serve:
    build:
      context: ..
      dockerfile: docker/Dockerfile.serve
    ports:
      - "8000:8000"
    environment:
      - WANDB_ENTITY=${WANDB_ENTITY}
      - WANDB_API_KEY=${WANDB_API_KEY}
