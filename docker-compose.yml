services:
  backend:
    build:
      context: .
      dockerfile: docker/Dockerfile.backend
    expose:
      - "8001"
    env_file: .env

  frontend:
    build:
      context: .
      dockerfile: docker/Dockerfile.frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
