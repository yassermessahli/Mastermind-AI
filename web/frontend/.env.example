# Base URL for the Django backend API, used by the Axios client.
# In Docker, leave empty so Nginx proxies /api/ to the backend container.
# For local development, point to the Django dev server.
VITE_API_URL=http://localhost:8000
